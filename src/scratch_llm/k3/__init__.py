"""K3 track — build & host Kimi K3 from scratch (docs/k3/ROADMAP.md).

Delegated-track modules live here (config, param_count, ...); the hand-built
mechanisms live in `k3/core/` (agents read-only — see HANDCRAFTED.md).
"""
