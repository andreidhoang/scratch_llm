"""A5 verifiable environments — the task/grader seam (:mod:`scratch_llm.envs.protocol`) plus the
concrete envs (Countdown, math) to come. The protocol is pure-Python and torch-free so the grader
contract imports on any box."""
